{
  "name": "salvation-museum",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "build:auction": "npm --prefix silent-auction install && npm --prefix silent-auction run build",
    "build:site": "npm run build && npm run build:auction && mkdir -p _site/asta && cp -r silent-auction/dist/. _site/asta/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}
