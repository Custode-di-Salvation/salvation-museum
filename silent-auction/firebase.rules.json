{
  "rules": {
    ".read": true,
    "auctionOpen": {
      ".read": true,
      ".write": false
    },
    "bids": {
      "$workId": {
        ".read": true,
        "$bidId": {
          ".write": "auth != null && (!data.exists() || data.child('ownerUid').val() === auth.uid)",
          ".validate": "newData.hasChildren(['pgName', 'amount', 'timestamp', 'ownerUid']) && newData.child('pgName').isString() && newData.child('amount').isNumber() && newData.child('amount').val() > 0 && newData.child('ownerUid').val() === auth.uid"
        }
      }
    }
  }
}
